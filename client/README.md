# client

The thin CLI a grid member installs. Loop: check in with declared
capabilities (model, provider, quant) → lease a job → run the review with
entirely local config (model choice, prompts, depth, cross-model loops) →
report results → check out whenever.

**Not started.** Define the registration/lease API in `hub/` first — this
loop is the product's spine.
